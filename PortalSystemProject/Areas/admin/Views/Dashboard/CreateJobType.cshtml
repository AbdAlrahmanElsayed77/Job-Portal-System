@model Domains.JobType
@{
    ViewData["Title"] = "Create Job Type";
}

<h2>Create Job Type</h2>

<form asp-action="CreateJobType" method="post" class="card p-4 shadow-sm">
    <div class="mb-3">
        <label asp-for="Name" class="form-label fw-bold"></label>
        <input asp-for="Name" class="form-control" placeholder="Enter job type (e.g. Full-time)" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-success">Create</button>
    <a asp-action="Industries" class="btn btn-secondary">Cancel</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
